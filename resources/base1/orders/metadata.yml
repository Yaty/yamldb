Structure:
    orderId:
        type: INT
        primary: yes
        defaultValue: no
        autoIncrement: yes
    customerId:
        type: INT
        primary: no
        defaultValue: no
        autoIncrement: no
    orderDate:
        type: STRING
        primary: no
        defaultValue: no
        autoIncrement: no
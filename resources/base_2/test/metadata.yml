Structure:
    trzt:
        type: STRING89
        primary: no
        defaultValue: no
        autoIncrement: no
    rzgsqg:
        type: STRING89
        primary: no
        defaultValue: no
        autoIncrement: no
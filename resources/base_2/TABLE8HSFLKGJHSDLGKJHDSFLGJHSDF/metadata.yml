Structure:
    lmdsjhgsdf:
        type: STRING
        primary: no
        defaultValue: no
        autoIncrement: no
    gfsg:
        type: STRING
        primary: no
        defaultValue: no
        autoIncrement: no
    sqgds:
        type: CHAR
        primary: no
        defaultValue: no
        autoIncrement: no
    fsg:
        type: STRING
        primary: no
        defaultValue: no
        autoIncrement: no
    sdg:
        type: STRING
        primary: no
        defaultValue: no
        autoIncrement: no